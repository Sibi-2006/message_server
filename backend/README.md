# Backend Server

This is the Node.js + Express backend for the Real-Time Messaging Platform. It includes authentication, user management, messaging, notifications, and real-time Socket.IO integration. The server is configured for Render free tier with cold‑start handling.
